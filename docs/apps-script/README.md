# Apps Script — survey webhook

Copy everything in **`survey-webhook.gs`** into a single file in the Apps Script editor (e.g. `Code.gs`), set `SPREADSHEET_ID`, run **`setupSheetHeaders`** once, then deploy as a **Web app** as described in [../GOOGLE_SHEETS_SURVEY.md](../GOOGLE_SHEETS_SURVEY.md).
